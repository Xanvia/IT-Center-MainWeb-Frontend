# Example environment variables for production
# Copy this file to .env.local (for development) or configure these in your deployment platform

# Backend URL that the client-side can access (must be publicly accessible)
NEXT_PUBLIC_BACKEND_URL=http://your-backend-domain.com:5100

# Backend URL that the server-side can access (can be internal network)
# In many cases, this will be the same as NEXT_PUBLIC_BACKEND_URL
INTERNAL_BACKEND_URL=http://your-backend-domain.com:5100

# NextAuth configuration
NEXTAUTH_URL=http://your-frontend-domain.com
NEXTAUTH_SECRET=your-nextauth-secret-key

# Optional: If using a different backend URL for Docker environments
# INTERNAL_BACKEND_URL=http://backend:5100
